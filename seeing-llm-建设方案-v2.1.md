# 看见大模型 · seeing-llm — 网站内容、设计与开发方案（v2.1 · 规格冻结）

> 定位：把"大模型是怎么造出来的"从论文与源码，变成**可点、可拖、可算给你看**的交互式教材。
> 技术底座复用 `seeing-single-cell` 的成熟架构（Next.js 14 静态导出 + GitHub Pages 部署链路已修复）。

---

## 〇、v1 → v2 变更摘要（先看这里）

| # | 您的决策 | 方案 v2 的落地方式 |
|---|---|---|
| 1 | 项目名 **seeing-llm** | 已定名，全文替换；仓库名 `seeing-llm`，中文名"看见大模型" |
| 2 | **纳入 DeepSeek V4.1 Flash** | 已核实：**2026-09-10 发布**，552B MoE / **全新 CED 架构**。轨道 A 从"V4 系列"改为**聚焦 V4.1 Flash**（V4 Pro 正被下线） |
| 3 | 聚焦 **K3 + V4.1 + Qwen3.8** | 轨道 A 由 **11 个模型 → 3 个深度解剖 + 1 张全景表**；模块页按这三个模型重新梳理为 **14 个** |
| 4 | **先用原图压缩** | 已**实测**可行（见 §1.4）：**101.2 MB → 约 2.5 MB，降幅 97.5%**。新增"位图 + 热区叠层"交互模式 |
| 5 | 算子**先做高频前 20** | 已核实 TorchCode 的真实标记分布：**🔥 只有 16 个**。前 20 = 16 个 🔥 + 4 个为三模型量身选的补充 |
| 6 | **先做中文** | i18n 骨架保留（加英文只是加数据），本期只产出中文内容 |
| 7 | 开发节奏 | 按 P0–P5 分期，**P1 先打通 Kimi K3 垂直切片** |

### 本轮核实中的三项重要发现

1. **Qwen3.8 确实存在，且方案 v1 的 Qwen3.5 已过时两代。** 您正文写的 3.8 是对的（末尾的 3.5 应为笔误）。**Qwen3.8-2.4T-A95B 已开源权重**，2.4T/A95B、92 层。
2. **`InfraTech` 已不足以作为最新模型的来源。** 该仓库 `models/` 下仍是 11 个目录，**没有 `qwen3_8`，也没有 `deepseek_v4_1_flash`**，最新的仍是 Qwen3.5 / DeepSeek V4。→ 焦点三模型里**两个的原图必须从官方渠道获取**（HF 模型卡 / ModelScope / 技术报告 PDF）。
3. **K3 与 Qwen3.8 的层骨架完全相同（23×4），可以直接并排对比。** 见 §4.1 的"双模型层墙"设计——这是本站最有原创价值的交互。

### v2 → v2.1 补充确认（4 项，见 §十一）

- 版本基准锁定 **Qwen3.8**（正文与末尾表述冲突，已核实取新）
- Qwen3.8 **27B** 在对比表留占位行，发布后补，不阻塞 P3
- **V4 Pro** 只留一行历史记录，不做解剖页
- 剩余 **21 个算子** 走「待补清单」长期滚动，P5 不承诺全量

---

## 一、材料调研结论

### 1.1 您给的三份材料，实际是四个资产源

| # | 您的描述 | 实际材料 | 内容体量 | 核心价值 |
|---|---|---|---|---|
| 1 | Kimi K3 架构可视化（知乎链接 1） | 长文译文《从 GPT-2 到 Kimi K3：2.8T 参数的前世今生》 | 约 8 章，含 12 段 PyTorch 代码、4 个公式 | **演进叙事最完整**：KV Cache → Linear Attention → DeltaNet → Gated DeltaNet → KDA → K3，是天然的"时间轴"素材 |
| 2 | DeepSeek V4.1 Flash（知乎链接 2） | 正文实为**第二篇 Kimi K3 模块解析**，但**"V4.1 Flash"这个名字是对的**——官方模型已于 2026-09-10 发布 | KDA / Gated MLA / AttnRes / Stable LatentMoE / MoonViT-V2 五大模块 | 模块拆解最深；同时引出了 V4.1 Flash 的官方来源 |
| 3 | MiniMind 训练全流程 | `jingyaogong/minimind` | 8 个训练阶段、8 个数据集、完整耗时成本表 | **训练流程最完整**，真实超参、真实数据格式、真实开销数字 |
| 4 | TorchCode 函数与策略 | `duoan/TorchCode` | 41 道题、5 大类、4 个难度档、自动判题 | **算子清单最权威**，41 个知识点可直接当目录用 |

> ✅ **上一版的疑问已澄清**：链接 2 的标题确实写的是 Kimi K3，但您要的 **DeepSeek V4.1 Flash 真实存在**且比 v1 里写的 V4 更新。已按官方技术报告重写规格。

### 1.2 焦点三模型规格（★ 本方案的核心事实基础）

三份规格均取自**官方技术报告 / 模型卡**，数据截止 **2026-09-14**。

#### A. Kimi K3（Moonshot AI · 2026-07-16 发布 · 权重 07-27 开放）

| 项 | 值 |
|---|---|
| 总参数 / 激活 | **2.8T / 104B**（世界首个开放的 3T 级模型） |
| 层数 | **93 层**（含 1 稠密层） |
| 层布局 | **23 × (3 × KDA + 1 × Gated MLA)** —— 即 69 KDA + 23 Gated MLA + 1 稠密 = 93 ✅ |
| 注意力隐藏维 / 头数 | 7168 / 96 |
| MoE | **896 专家，每 token 激活 16 个**（稀疏度 1.8%），2 个共享专家；Latent 维度 3584；专家隐藏维 3072 |
| 上下文 | 1,048,576（1M） |
| 激活函数 | **SiTU-GLU** |
| 视觉 | MoonViT-V2（401M，原生多模态） |
| 量化 | **MXFP4 权重 / MXFP8 激活**，自 SFT 阶段即 QAT |
| 词表 | 160K |
| 关键创新 | **KDA**（通道级门控的 delta 规则，源自 arXiv:2510.26692）、**AttnRes**（把残差"等权累加"换成跨深度 softmax 检索）、**Stable LatentMoE**（Quantile Balancing + Per-Head Muon） |
| 效果 | 官方称缩放效率相比 K2 提升 **2.5×**；1M 上下文下解码速度最高 **6.3×**；Kimi Linear 论文称 KV Cache 最多削减 **75%** |
| 许可 | **Kimi K3 License（非 MIT/Apache）** —— MaaS 业务连续 12 个月营收 > 2000 万美元需另签协议；月活 > 1 亿或月营收 > 2000 万美元的产品需显著署名 |

> 📌 **一处口径已澄清**：第三方资料（DeepWiki）写"Gated MLA 24 层"，但按 23×4+1 的官方布局推算应为 **23 层**（69+23+1=93）。实现页以 `k3_tech_report.pdf` 为准，站内此处标注"以官方技术报告为准"。

#### B. DeepSeek V4.1 Flash（DeepSeek · 2026-09-10 发布 · 已在 HF 开源 + 技术报告）

| 项 | 值 |
|---|---|
| 骨干参数 | **552B**（MoE）；上下文 1M |
| 激活（**非对称**） | **Prefill/输入 8B，Decode/输出 16B** |
| 结构 | **CED（Causal Encoder-Decoder）**：**40 层 = 20 层因果编码器 + 20 层解码器** |
| 核心机制 | 解码器的**全局 KV Cache 由编码器最后一层的隐藏态投影而来**，不是逐解码层自己算出来的 → 输入侧几乎不花算力 |
| 注意力 | **CSA2（Compressed Sparse Attention 2）**，每层静态三模式 **Full / Reindex / Reuse**，跨层共享主 KV 与 indexer K、复用 Top-K 稀疏索引；前 2 层用 **SWA**；解码器加 **Hierarchical Sparse Indexer** 限制深层索引成本 |
| KV 压缩 | **FP4 主 KV**（E2M1，每 16 通道一个 E4M3 scale）→ **890 字节/token**（≈ 上一代 V4-Flash 的 1/4）；SWA Bounded Replay 使持久 KV ≈ **1/8**；相对初代累计 **437×** |
| MoE | **1 共享专家 + 384 路由专家**，每 token 激活 **6 个路由专家** |
| 其他模块 | **Single-Pass mHC**（改进残差流混合，Mega-mHC kernel）、**Engram**（196B 条件记忆，按 token 稀疏查表）、**DSpark**（半自回归草稿 + 置信度调度验证的投机解码） |
| 视觉 | **DeepSeek-ViT**（从零训练，2D-RoPE + 3×3 pixel-unshuffle 降采样）+ 两层 MLP projector |
| 预训练 | 多模态语料 **45T tokens** 从零训练；64K 序列训稀疏注意力；**34T tokens 处扩展到 1M** |
| 后训练 | **SFT → RL → OPD（on-policy distillation）** 标准范式、无算法改动；所有实质变化都在**数据管线**（大规模合成 Agent 任务与环境） |
| 推理控制 | `reasoning_effort` **连续整数 1–100** |
| 价格 | 输入 $0.15 / 输出 $0.60（每百万 token，闲时）；峰时翻倍；**缓存命中 $0.003**（约为未命中的 1/50） |
| 时事 | V4 Pro 自 **2026-09-14 12:00（北京）** 起全部路由到 V4.1 Flash 并按下线处理 |

#### C. Qwen3.8-2.4T-A95B（阿里 · 权重已在 ModelScope 开放）

| 项 | 值 |
|---|---|
| 总参数 / 激活 | **2.4T / 95B** |
| 层数 / 隐藏维 | **92 层** / 8192 |
| 层布局 | **23 × (3 × (Gated DeltaNet → MoE) → 1 × (Gated Attention → MoE))** |
| Gated DeltaNet | 线性注意力头 **128 (V) / 16 (QK)**，head dim 128 |
| Gated Attention | **64 Q 头 / 4 KV 头**（GQA），head dim 256，**RoPE dim 64** |
| MoE | **512 专家，10 路由 + 1 共享**，专家中间维 2048 |
| 上下文 | 原生 **262,144**，可扩展至 **1,010,000** |
| 其他 | 多步 **MTP（Multi-Token Prediction）** 训练；默认思考模式（不可关闭）；`reasoning_effort` = xhigh/medium/low |
| 词表 | 248,320（padded） |
| 姊妹版本 | Qwen3.8-Max（云端版，加视觉输入、1M 默认上下文、内置工具） |

> 🎯 **这三份规格放在一起，本身就是一篇好文章**（见 §4.1 末尾"同一问题的三种解法"）。

### 1.3 `InfraTech` 仓库：仍是内容金矿，但**已滞后**

**仍然是好用的部分**（截至本次抓取）：

- **11 个模型卡**：`deepseek_v3` / `deepseek_v3_2` / `deepseek_v4` / `glm_5` / `kimi_k_2` / `kimi_k_2_5` / `kimi_k_3` / `minimax_m_2_5` / `qwen3_5` / `qwen3_vl` / `step_3_5_flash`
- **`models/modules/`**：模块级专题（RoPE 原理 1D→3D、MLA 吸收矩阵、DSA 设计、线性注意力、VLM 融合、`rope_principle.ipynb`）
- **`docs/`**：RL 框架专题（vime / slime × vLLM 权重同步、共卡模式、训推一致性）、MoE-RL、分布式集合通信、梯度近似与雅可比
- **工具**：LLM 显存计算、MFU 计算器、**交互式 DeepSeek V3 计算图**、PyTorch 显存可视化
- **`llm_infer/`**：推理练习

**已经滞后的部分**（必须在方案里绕开）：

| 缺口 | 影响 | 应对 |
|---|---|---|
| **无 `qwen3_8`** | Qwen3.8 的规格与架构图要另找 | 以 ModelScope 模型卡 + Qwen 官方博客为准；图从官方渠道获取 |
| **无 `deepseek_v4_1_flash`** | V4.1 Flash 的 Figure 3 要另找 | 从官方技术报告 PDF 提取（`pdfimages` / PyMuPDF） |
| **最高频的 Qwen 卡仍是 `qwen3_5`** | 全站"最新"叙事若依赖它会失真 | 全景表里的数字全部以官方来源标注，InfraTech 仅作**历史模型与专题文章的参考** |

> **结论**：InfraTech 从"内容金矿"降级为"**历史模型与专题文章的重要参考**"。焦点三模型的一手来源必须换成 **HF 模型卡 + ModelScope + 官方技术报告**。

### 1.4 图片体量的实测结果（支撑您"先用原图压缩"的决策）

我实际下载了 Kimi K3 架构图并做了压缩测试，结论比预期更好：

**实测（Kimi K3 架构图）**

| 处理方式 | 体积 | 相对原图 |
|---|---|---|
| 原图 JPG（**10850 × 12619**） | **10.22 MB** | 100% |
| 原尺寸 WebP q80（不缩放） | 1,390 KB | 13.6% |
| 缩到 2400px 宽 + WebP q80 | 302 KB | 2.9% |
| **缩到 1600px 宽 + WebP q80（推荐）** | **188 KB** | **1.8%** |
| 缩到 1200px 宽 + WebP q85 | 149 KB | 1.4% |
| 缩到 1600px 宽 + AVIF q60 | 122 KB | 1.2% |
| 缩略图 800px + WebP q75 | 63 KB | 0.6% |

**🔑 关键洞察：降幅主要来自"降分辨率"，不是"换格式"。**
原图 10850px 宽是给放大细看/印刷用的，而网页在 Retina 屏上最多也就需要 1600–2400px。
所以流水线的重点不是"选 WebP 还是 AVIF"，而是**先把 10850px 降到展示所需宽度**——这一步就吃掉了 87% 的体积。

**全量基线（实测统计，非估算）**

| 项 | 数值 |
|---|---|
| 图片总数 | **17 张**（`deepseek_v3_2` 有 3 张、`qwen3_vl` / `qwen3_5` / `deepseek_v3` 各 2 张） |
| 原始总量 | **约 101.2 MB**（v1 估的 60 MB 是低估） |
| 单张最大 | `deepseek_v4_architecture.jpg` **11.81 MB** |
| 压缩后预估总量 | **约 2.5 MB**（按每张 100–160 KB 主图 + 缩略图计） |
| **降幅** | **约 97.5%** |

> 结论：**您的"先用原图压缩"决策完全可行**，60→2.5 MB 的降幅让 GitHub Pages 完全没有压力。代价是图**不可交互**——我们用 §6.3 的"**位图 + 热区叠层**"模式把大部分交互性补回来。

---

## 二、网站定位与目标用户

**一句话定位**：`seeing-single-cell` 教你"怎么看懂单细胞数据"，本网站教你"**怎么看懂大模型本身**"——从宏观架构，到中观训练流程，再到微观算子实现。

**三层目标用户**：

| 用户 | 痛点 | 网站给什么 |
|---|---|---|
| 算法/工程入门者 | 读得懂论文名词，但不知道模块长什么样、数据怎么流 | 可交互的架构解剖：点开每个模块看到张量形状与计算流 |
| 准备面试的求职者 | 要手写 softmax/attention/MoE，但缺少反馈与直觉 | 算子可视化：小矩阵上逐步执行，看见每一步中间结果与形状变化 |
| 想复现小模型的爱好者 | 训练脚本能跑，但不清楚每个阶段在干什么、要花多少钱 | 训练流程流水线：真实数据格式 + 真实超参 + 开销计算器 |

**差异化定位（相对既有资源）**：

| 资源 | 它的强项 | 我们的差异 |
|---|---|---|
| `InfraTech` | 模型卡 + 深度专题文章 | 它是**文章合集**，我们是**可操作的交互**；且它已滞后于最新模型 |
| `TorchCode` | 41 题在线判题、真刀真枪练 | **它是练习场，本站是理解场**——我们只做讲解 + 可视化，页面底部导流到它去练 |
| 知乎技术长文 | 叙事完整、有深度 | 长文是**线性的**；我们把它拆成**可点、可拖、可算**的节点，并加上三轨交叉链接 |

---

## 三、信息架构（聚焦三模型）

### 3.1 三轨主干

```
看见大模型 seeing-llm
│
├─ 轨道 A · 架构解剖  Anatomy       ← 材料 1、2（Kimi K3 / DeepSeek V4.1 Flash / Qwen3.8）
│    A1 模型全景对比表 × 1           12 个模型横向对比，其中 3 个有详情页
│    A2 单模型解剖页 × 3             K3 / V4.1 Flash / Qwen3.8
│    A3 模块详解页 × 14              ★ 按三模型重新梳理（见 4.1）
│    A4 横向对比专题 × 2             MoE 稀疏度 · KV Cache 三条路线
│    A5 架构演进时间轴 × 1           GPT-2 → … → K3 / V4.1 Flash / Qwen3.8
│
├─ 轨道 B · 训练全流程  Pipeline     ← 材料 3（MiniMind）
│    B1 流水线总览 × 1               可点击的 8 阶段流程图
│    B2 阶段详解页 × 8               Pretrain / SFT / KD / LoRA / DPO / PPO·GRPO·CISPO / Agentic RL / ToolCall
│    B3 交互实验室 × 3               分词 / 下一个 token 预测 / 训练开销计算器
│
└─ 轨道 C · 算子与策略  Operators    ← 材料 4（TorchCode 高频前 20）
     C1 算子清单 × 1                 20 题 × 难度 / 频率 / 进度
     C2 算子详解页 × 20              形状流转 + 数值分步执行 + 参考实现 + 陷阱
```

### 3.2 页面清单

| 板块 | 页数 | 数据驱动？ |
|---|---|---|
| 首页 + 关于 / 致谢 / 术语表 / 404 / 概念地图 | 6 | 否 |
| A1 模型对比表 | 1 | ✅ 数据表 |
| A2 模型解剖页 | 3 | ✅ 模板 + `generateStaticParams` |
| A3 模块详解页 | 14 | 半数据驱动（交互组件各异） |
| A4 横向对比专题 | 2 | 否 |
| A5 演进时间轴 | 1 | ✅ 数据驱动 |
| B1 流水线总览 | 1 | 否 |
| B2 阶段详解页 | 8 | ✅ 模板 + 数据 |
| B3 交互实验室 | 3 | 否 |
| C1 算子清单 | 1 | ✅ 数据表 |
| C2 算子详解页 | 20 | ✅ 模板 + 数据 |
| **合计** | **约 60 页** | **其中 33 页由同一套模板 + 数据文件生成** |

> **关键设计决策**：A2 / B2 / C2 共 31 页全部走 **`generateStaticParams` + 内容数据文件**（见 §7.2）。每个算子/模型/阶段写成一份结构化数据，页面模板只写一次。新增一个算子 = 加一个数据条目。

---

## 四、内容设计（逐板块）

### 4.1 轨道 A · 架构解剖

#### A1 模型全景对比表（入口页，第一眼抓人）

12 个模型横向对比：参数/激活、上下文、注意力类型、MoE 配置、发布时间、许可类型。

| 模型 | 注意力 | MoE 配置 | 总参/激活 | 上下文 | 发布 |
|---|---|---|---|---|---|
| DeepSeek V3 | MLA + MoE | — | 671B / 37B | 128K | 2024.12 |
| Kimi K2 | MLA + MoE | — | 1T / 32B | 128K | 2025.07 |
| DeepSeek V3.2 | MLA + DSA | — | 685B 级 | 128K | 2025.12 |
| Qwen3-VL | DeepStack + Interleaved-MRoPE | — | 32B · 235B/22B | 256K | 2025.10 |
| DeepSeek V4 | CSA + HCA 混合 | — | 1.6T/49B · 284B/13B | 1M | 2026.04 |
| Qwen3.5 | Gated DeltaNet + Gated Attn | 397B / 17B | 262K | 2026.02 |
| GLM 5 | MLA(DSA) + MoE | — | 744B / 40B | 200K | 2026.02 |
| MiniMax M2.5 | GQA + MoE | — | 229B / 10B | 200K | 2026.02 |
| Step 3.5 Flash | GQA + SWA + MoE + MTP | — | 196B / 11B | 256K | 2026.02 |
| **★ Kimi K3** | **KDA + Gated MLA** | **896 → 16 (+2 共享)** | **2.8T / 104B** | **1M** | **2026.07** |
| **★ DeepSeek V4.1 Flash** | **SWA + CSA2 + CED** | **384 → 6 (+1 共享)** | **552B / 8B·16B** | **1M** | **2026.09** |
| **★ Qwen3.8** | **Gated DeltaNet + Gated Attn** | **512 → 10 (+1 共享)** | **2.4T / 95B** | **262K→1M** | **2026.09** |

- 交互：按任意列排序；按"注意力类型"筛选（MLA / KDA / DeltaNet / CSA / GQA / SWA）；**★ 标记的 3 个可进入详情页**，其余标"待补"
- **视觉化**：一张"参数量 vs 发布时间"的散点图，气泡大小 = 激活参数量。**GPT-2(124M) → K3(2.8T) 是 22580 倍**，这个增长一眼可见

#### A2 单模型解剖页 × 3（统一四层递进结构）

1. **规格卡**：官方参数表，带来源链接与"数据截止 2026-09-14"
2. **架构图 + 热区**：**用官方原图（压缩后）**，上面覆盖一层透明热区（§6.3）。点 KDA 区块 → 侧栏展开说明 + 跳转模块页
3. **逐层剖面墙**（本页最大亮点）：见下
4. **与前代 / 与同侪对比**：diff 视图

##### ★★ 亮点交互：双模型层墙（K3 ↔ Qwen3.8 逐层对齐）

这是本轮调研挖出的**最有原创价值的对比**：

```
Kimi K3      93 层 = 1 稠密 + 23 × (3 × KDA            + 1 × Gated MLA)
Qwen3.8      92 层 =            23 × (3 × Gated DeltaNet + 1 × Gated Attention)
                     └──────────── 骨架完全相同 ────────────┘
```

**两者都是"每 4 层：3 层线性注意力 + 1 层全注意力"，层次节奏逐层对齐。** 差别在实现：

| | Kimi K3 | Qwen3.8 |
|---|---|---|
| 线性注意力 | **KDA**（通道级门控的 delta 规则） | **Gated DeltaNet**（同为 delta 系） |
| 全注意力 | **Gated MLA**（潜在注意力 + 输出门控） | **Gated Attention**（64 Q / 4 KV，GQA + 门控） |
| MoE | 896 → 16 + 2 共享 | 512 → 10 + 1 共享 |

**交互设计**：左右两列层条**逐层水平对齐**，中间用连线标出"这一层两者在用不同实现做同一件事"。拖动一个"层游标"同时扫过两个模型，实时对比该层的计算路径。**"同一骨架、两种实现"——这个交互在任何现有资料里都没有。**

同时保留 v1 的**单模型层剖面墙**：

- 把 K3 的 93 层渲染成一列可滚动、可 hover 的层条，每层标注 `稠密` / `KDA` / `Gated MLA` / `MoE`
- **一眼看出 23 个四层宏循环的节律**（3 KDA + 1 MLA，每 12 层一次 AttnRes 边界）
- 交互：hover 看配置；点"只看注意力层"过滤；拖动层指示器看数据流经不同层类型的差异
- V4.1 Flash 是**另一种结构哲学**：40 层竖切成 20 编码 + 20 解码，用**"纵向切分"**而非"纵向堆叠"——这张图要画出"输入只走上半、输出的 KV 从上半投影下来"的非对称性

#### A3 模块详解页 × 14（按三模型重新梳理）

**Kimi K3 侧（5）**

| 模块 | 交互设计 |
|---|---|
| **KDA（Kimi Delta Attention）** | ① **状态矩阵递推演示**：D×D 状态 S 的"读旧→减旧→写新"，可单步/播放 ② **α 衰减滑条**：α=1 退化为纯 Delta rule，α=0 清空记忆 ③ **通道级 vs 标量门控对比**：1 个 α vs d_k 个 α，热力图直观对比（这正是 KDA 相对 Gated DeltaNet 的核心改进） |
| **Gated MLA** | ① 头数 64→96 的 Q/K/V/O 宽度对比 ② **output gate 演示**：`sigmoid(g_proj(h))` 的门控，滑条控制 gate 值 ③ **NoPE 取舍**：切 RoPE / NoPE，看 1M 上下文的外推行为 |
| **AttnRes** | ① **标准残差 vs AttnRes 并排**：等权累加 vs softmax 加权聚合 ② **α 权重热力图**：随层数变化，看深层如何"回看"早期层 ③ Block AttnRes 的边界可视化（官方数据：额外算力 <2% 换来训练效率 +1.25×） |
| **Stable LatentMoE** | ① **路由热力图**：896 专家 × N token，看 top-16 分布 ② **Latent 维度流**：7168→3584→(专家内 3072)→7168 动画 ③ **Quantile Balancing vs 传统负载均衡**的专家利用率对比 |
| **SiTU-GLU** | 函数曲线实验室：SiTU / SwiGLU / SiLU / GELU 并排，β 可调，看极端稀疏下的限幅行为 |

**DeepSeek V4.1 Flash 侧（6）**

| 模块 | 交互设计 |
|---|---|
| **CED（非对称编解码）** | ★ **本页最有教学价值**：滑块控制"输入长度 / 输出长度"，实时看**为什么输入只花 8B、输出花 16B**。画出"prefill 只走编码器 20 层、decode 的全局 KV 从编码器末层投影而来"，对比常规 decoder-only 的重复计算量 |
| **CSA2（压缩稀疏注意力 2）** | ① **三模式演示**：给 40 层逐层分配 Full / Reindex / Reuse，看"哪些层真算、哪些层复用别人算好的索引" ② **Hierarchical Sparse Indexer**：第一层 Full 建候选池，后续层只在池内检索，拖动上下文长度看深层索引成本**不随长度线性增长** ③ 共享 KV 与 indexer K 的跨层流 |
| **KV Cache 压缩** | “890 字节/token” 的逐项拆解：FP4(E2M1) + 每 16 通道一个 E4M3 scale + SWA Bounded Replay + 层间共享 → 合计 1/4 HBM、1/8 SSD、相对初代 437× |
| **Engram（条件记忆）** | 196B 参数、按 token 稀疏查表的"第三类记忆"：对比①注意力（短期）②权重内知识（长期）③Engram（可查的关联字典）三者的成本曲线 |
| **Single-Pass mHC** | 残差流混合的改进：对比标准残差 / 双向 HC / 单遍 mHC 的信号传播稳定性 |
| **DSpark（投机解码）** | 草稿-验证流程动画：半自回归草稿 + 置信度调度验证，滑条调接受率看加速比 |

**Qwen3.8 侧（3）**

| 模块 | 交互设计 |
|---|---|
| **Gated DeltaNet** | 与 KDA 页**互为对照**：同一 delta 规则、不同门控粒度。128 V 头 / 16 QK 头的非对称头配置可视化（为什么 K 头少于 V 头——这正是 InfraTech 专题文章的问题） |
| **Gated Attention（64Q / 4KV）** | GQA 的分组映射动画：64 个 Q 头如何共享 4 组 KV；head dim 256、RoPE dim 64 的维度账 |
| **MTP（多 token 预测）** | 一次前向预测多个 token 的训练与推理差异；对比 K3/V4.1 Flash 各自的加速机制 |

**A4 横向对比专题 × 2（★ 本站最原创的内容）**

| 专题 | 内容 |
|---|---|
| **MoE 稀疏度对比** | 896→16(1.8%) / 512→10+1(2.0%) / 384→6+1(1.6%) 三家并排；专家粒度（3072 vs 2048 vs —）× 数量 × 激活量的取舍；为什么 K3 需要 Quantile Balancing 而小稀疏度不需要 |
| **KV Cache 的三条路线** | **同一问题、三种解法**：① K3/Qwen3.8 的**线性注意力**——用固定大小的递归状态彻底绕开增长 ② V4.1 Flash 的**压缩稀疏 + FP4 + 层间共享**——保留全注意力但把每 token 压到 890 字节 ③ 传统 GQA+SWA 的**头共享 + 窗口**。一张图说清三种哲学 |

> 这两页是"别人给你三份材料，我们给你一张能走通的路"的最佳体现——**没有一篇现有资料把这三个模型放在一起比。**

#### A5 架构演进时间轴

横向时间轴，把材料 1 的叙事价值最大化：

`GPT-2 (2019) → Linear Attention → DeltaNet → Chunked DeltaNet → Gated DeltaNet → KDA/Kimi Linear → Kimi K3 / V4.1 Flash / Qwen3.8 (2026)`

每个节点展开：① 一句话说清"解决了上一步的什么问题" ② **核心公式对比卡片**（KaTeX）③ 一段最小可读的 PyTorch 代码。串联主题：**"模型存什么、怎么更新、怎么取回"**。

配套 3 个"难点专页"：
- **Chunked 并行化**：C=1 / C=64 / C=N 三种粒度对比动画
- **KV Cache 的增长**：拖动序列长度看显存/带宽的 O(N) 曲线（对比线性注意力的常数曲线）
- **householder 变换与一阶线性递推**：步骤化推导

### 4.2 轨道 B · 训练全流程

**B1 流水线总览**：可点击的 8 阶段流程图，每节点标注 **数据规模 / 3090 单卡耗时 / 成本**（MiniMind 的真实数字是极强的可信度锚点）。

| 阶段 | 脚本 | 数据 | 3090 耗时 | 成本 |
|---|---|---|---|---|
| Pretrain | `train_pretrain.py` | `pretrain_t2t_mini` 1.2GB | ≈1.21h | ≈1.57￥ |
| SFT | `train_full_sft.py` | `sft_t2t_mini` 1.6GB | ≈1.10h | ≈1.43￥ |
| ToolCall | — | — | ≈0.9h | ≈1.17￥ |
| RLAIF | `train_grpo.py` 等 | `rlaif.jsonl` 24MB | ≈1.1h | ≈1.43￥ |
| KD | `train_distillation.py` | 教师-学生 | — | — |
| LoRA | `train_lora.py` | `lora_xxx.jsonl` | CPU 可跑 | — |
| DPO | `train_dpo.py` | `dpo.jsonl` 53MB | — | — |
| Agentic RL | `train_agent.py` | `agent_rl.jsonl` 86MB | — | — |

**B2 阶段详解页 × 8**，每页统一四段：① 这一阶段在干什么（白话）② **真实数据样例**（贴 jsonl 的一行）③ **损失函数**（KaTeX）+ **超参表**（8 层 / d_model 768 / 词表 6400 / 32768 上下文 / q_heads 8 / kv_heads 4）④ **原生 PyTorch 实现**（MiniMind 的核心卖点是**零高层抽象**，必须体现）。

> 💡 **与轨道 A 的天然接口**：MiniMind 的 PPO/GRPO/CISPO 阶段正好能对上 **V4.1 Flash 的 OPD（on-policy distillation）**——V4.1 Flash 的后训练就是 `SFT → RL → OPD`。这是 B 轨与 A 轨最实的一条连线。

**B3 交互实验室 × 3**

| 实验室 | 设计 | 数据依赖 |
|---|---|---|
| **分词实验室** | 输入中英文实时看 BPE 分词；并排对比 MiniMind(6400) / Qwen3.8(248320) / Llama3(128000) 的分词差异与 token 数 | 预计算 tokenizer 样例 |
| **下一个 token 预测** | prompt → top-k 候选概率条形图 → 拖 **temperature / top-k / top-p** 看分布被裁剪与重归一化（**与算子页 `topk_sampling` 共用同一组件**） | 预计算 logits JSON（绝不在主线程跑推理） |
| **训练开销计算器** | 选模型规模 + 数据量 + GPU → 估算时长与成本（以 MiniMind 实测标定） | 纯公式 |

### 4.3 轨道 C · 算子与策略（高频前 20）

**关于"前 20"的重要澄清**：我核实了 TorchCode 的真实标记——它的频率分三档：**🔥 = 面试极可能遇到（16 个）、⭐ = 常被问（14 个）、💡 = 新兴/差异化（11 个）**，合计 41。

所以"**高频前 20**"的准确构成是：**16 个 🔥 全部收录 + 4 个为焦点三模型量身挑选的补充**。

| # | 来源 | 算子 | 难度 | 频率 |
|---|---|---|---|---|
| 1 | Fundamentals | ReLU | Easy | 🔥 |
| 2 | Fundamentals | Softmax | Easy | 🔥 |
| 3 | Fundamentals | Linear Layer | Medium | 🔥 |
| 4 | Fundamentals | LayerNorm | Medium | 🔥 |
| 5 | Fundamentals | Cross-Entropy Loss | Easy | 🔥 |
| 6 | Fundamentals | Dropout | Easy | 🔥 |
| 7 | Fundamentals | Embedding | Easy | 🔥 |
| 8 | Fundamentals | Conv2d | Medium | 🔥 |
| 9 | Fundamentals | Linear Regression | Medium | 🔥 |
| 10 | Attention | Scaled Dot-Product Attention | Hard | 🔥 |
| 11 | Attention | Multi-Head Attention | Hard | 🔥 |
| 12 | Attention | Causal Self-Attention | Hard | 🔥 |
| 13 | Attention | KV Cache Attention | Hard | 🔥 |
| 14 | Attention | RoPE | Hard | 🔥 |
| 15 | Inference | Top-k / Top-p Sampling | Medium | 🔥 |
| 16 | Inference | Beam Search | Medium | 🔥 |
| — | — | — **以上为全部 🔥，以下 4 个为本站补充** — | — | — |
| 17 | Fundamentals | **Mixture of Experts** | Hard | ⭐ |
| 18 | Attention | **Grouped Query Attention** | Hard | ⭐ |
| 19 | Attention | **Linear Attention** | Hard | 💡 |
| 20 | Architecture | **GPT-2 Block** | Hard | ⭐ |

**为什么补这 4 个**：

- **MoE**（17）——焦点三模型**全是 MoE**，是 A 轨与 C 轨之间最短的一条连线
- **GQA**（18）——Qwen3.8 的"1 层全注意力"就是 64Q/4KV 的 GQA；Step 3.5 Flash 也是 GQA
- **Linear Attention**（19）——KDA 与 Gated DeltaNet 的**共同祖先**，不讲它没法讲 K3 和 Qwen3.8
- **GPT-2 Block**（20）——架构母版，把前面所有组件串成一个完整 Transformer

**C2 算子详解页 × 20**，统一四段结构：

1. **问题与签名**：要实现的函数、输入输出 shape
2. **形状流转图**：输入 shape → 中间 shape → 输出 shape，逐步骤动画高亮
3. **数值分步执行**（核心交互）：小尺寸真实数据（如 4×4 矩阵）逐步执行，显示每步中间张量
   - Softmax：输入 `[1000, 1001, 1002]` → 朴素实现 overflow → 减 max 后正常（可拖动输入值实时看）
   - Attention：可编辑 Q/K 的 4×4 小矩阵 → 实时看 `QKᵀ/√d` 热力图与 softmax 权重
   - MoE：8 个 token × 16 个专家的小路由表，拖动 top-k 看专家负载变化
   - RoPE：位置 ↔ 旋转角的 2D 旋转动画
   - GQA：64 个 Q 头到 4 组 KV 的分组映射动画
   - Beam Search：搜索树的展开与剪枝动画
4. **参考实现 + 常见陷阱**（数值稳定性、train/eval 差异、广播错误、梯度断流）

**剩余 21 个算子**（⭐ 10 个 + 💡 11 个）留到 P5 或后续版本，页面底部列为"待补清单"，让用户知道还有哪些。

> **刻意不做的事**：TorchCode 的在线判题需要后端（Jupyter + 容器）。本站纯静态导出，**不复制判题系统**，只做"讲解 + 可视化"，并在每页底部引导用户去 TorchCode / Colab 真刀真枪练。

---

## 五、核心亮点：三轨知识网络

三份材料本身是独立的三堆内容。**把它们连成一张网，才是这个站真正的独特性。**

建立**双向交叉链接**：每个模块/阶段数据里加一个 `relatedOps: string[]` 字段，页面上渲染成"🔗 想搞懂这个？去看 `linear_attention` 的实现"的卡片。

| 在轨道 A 看到 | 链接到轨道 C |
|---|---|
| KDA | `linear_attention`, `rope` |
| Gated DeltaNet（Qwen3.8） | `linear_attention`, `gqa` |
| Gated MLA / Gated Attention | `mha`, `attention`, `kv_cache`, `gqa` |
| CSA2 / SWA | `causal_attention`, `sliding_window`（待补）, `kv_cache` |
| CED | `attention`, `kv_cache` |
| Stable LatentMoE / MoE | `moe`, `topk_sampling` |
| SiTU-GLU / FFN | `swiglu_mlp`（待补）, `gelu`（待补） |
| AttnRes | `layernorm`, `rmsnorm`（待补） |
| DeepSeek-ViT / MoonViT-V2 | `vit_patch`（待补） |
| Engram | `embedding` |
| DSpark | `speculative_decoding`（待补） |
| MXFP4 量化 | `int8_quantization`（待补） |

| 在轨道 B 看到 | 链接到轨道 C |
|---|---|
| Pretrain / SFT | `cross_entropy`, `adam`（待补）, `cosine_lr`（待补） |
| KD / **OPD** | `cross_entropy`（KL 变体） |
| LoRA | `lora`（待补） |
| DPO / PPO / GRPO | `dpo_loss` / `ppo_loss` / `grpo_loss`（待补） |
| 采样/生成 | `topk_sampling`, `beam_search` |

**与 v1 的差别**：v1 里有相当比例的链接指向"待补"算子——这暴露了 v1 的"前 20"清单（按纯 🔥 频率选）与三轨联动的需求不匹配。**v2 补的 4 个算子（MoE / GQA / Linear Attention / GPT-2 Block）正是为了把断链补上**：A 轨的高频链接对象现在是实链接，而不是"待补"。

再加一个**全局"概念地图"页**：把三大板块的节点画成一张可缩放的力导向图，点击任意节点跳转。这张图本身就是"我该学什么"的导航。

> **一句话总结**：别人给你三份材料，我们给你一张**能走通的路**。

---

## 六、视觉与交互设计

### 6.1 风格

从 seeing-single-cell 的"生物学科研感"转向"**系统工程师美学**"——等宽字体、网格/矩阵元素、数据流线条。

| 维度 | 方案 |
|---|---|
| 基调 | 深色为主（契合终端/IDE 语境），保留浅色切换 |
| 主色 | 深蓝紫 `#534AB7` 系（与 seeing-single-cell 的暖橙区分，形成品牌矩阵） |
| 辅助色 | 紫 `#7F77DD` / 青 `#1D9E75` / 琥珀 `#EF9F27`（区分层类型、专家、阶段） |
| 字体 | 正文 Inter / 思源黑体；**代码与公式 JetBrains Mono** |
| 标志性元素 | ① 层叠的"张量块" ② 数据流虚线动画 ③ 矩阵网格背景 |
| 动效原则 | 只用于**表达状态变化**（张量形状、状态更新、路由选择），不做装饰性动画 |
| 可访问性 | 颜色不单独承载信息（层类型同时用形状/标签区分）；`prefers-reduced-motion` 时关动画 |

### 6.2 交互组件的统一规范（从 seeing-single-cell 提炼）

每个组件三件套：**可调参数（滑条/输入）→ 实时可视化 → 数值读数**。

- 大计算量一律预计算或放 Web Worker。**第六章 UMAP 的教训：主线程同步重算 = 页面假死 = 用户认为"加载失败"**
- 首次进入必须秒开：交互组件默认渲染**静态结果**，用户操作后才触发实时计算，且加 debounce
- 所有 fetch 带 2 次自动重试 + 显式"重试"按钮

### 6.3 ★ 新增模式：位图 + 热区叠层（配合"先用原图压缩"的决策）

因为您选择先用原图压缩（而非自绘 SVG），架构图本身是**静态位图**。为了不让"点开模块看详情"这个核心交互丢失，采用**叠层方案**：

```
┌─────────────────────────────────────┐
│  <div class="fig-wrap">             │
│    <img src="k3.webp" />     ← 压缩后的位图（188 KB）
│    <svg class="hotzones">    ← 透明 SVG 覆盖层
│      <a href="/modules/kda">  ← 与图片上模块位置对齐的透明热区
│      <a href="/modules/attnres">
│    </svg>                            │
│  </div>                              │
└─────────────────────────────────────┘
```

**实现要点**：

- 热区用**百分比坐标**（`viewBox="0 0 100 100"` + `preserveAspectRatio="none"`），图片缩放时热区自动跟随，无需重算
- 热区默认透明，hover 时显示半透明高亮 + 模块名 tooltip → **视觉上像"图片自带的交互"**
- 用**图片的自然宽高比**做容器（`aspect-ratio`），避免布局抖动
- 热区坐标的标注成本极低：每个模型只需量一次（用 §8 的 `annotate_hotzones.py` 辅助工具，可视化点选）

**这个模式的价值**：拿到了自绘 SVG 约 **80% 的交互收益**（可点击、可跳转、可 hover），但工作量只有 **10%**。代价是模块形状不可动态变化（比如"头数 64→96"这类**结构性**演示仍需要额外的自绘小图）——所以 **A3 模块页的交互组件仍然自绘**，只有 **A2 的整机架构图**用位图。

**后续升级路径**：P5 之后若有余力，可按 §10 的降级方案把 K3 一张图升级为自绘 SVG，验证"位图→自绘"的增量收益，再决定是否铺开。

---

## 七、技术架构

### 7.1 技术选型

**直接复用 seeing-single-cell（已验证、已修复）**

| 项 | 内容 |
|---|---|
| 框架 | Next.js 14.2.5 App Router + `output: 'export'` 静态导出 |
| 部署 | `.github/workflows/deploy.yml` → gh-pages（**CI 的 0 秒失败 bug 已修复，直接复制这份文件**） |
| 样式 | Tailwind CSS 3.4 + `tailwindcss-animate` |
| 图标 | `lucide-react` |
| 公式 | `katex` |
| 主题 | `ThemeToggle` 深/浅色切换 |
| 背景 | `ParticleBackground`（改造为矩阵网格风格） |
| 访问统计 | `VisitorCounter.tsx` + 不蒜子 busuanzi（**已验证可用，直接搬**） |
| 子路径 | `basePath` + `trailingSlash`（**踩过的坑：`next/script` 与 fetch 必须手动加前缀**） |
| i18n | `LangContext` + `translations.json` **骨架保留**，本期只填中文 |

**需要新增**

| 需求 | 选型 | 理由 |
|---|---|---|
| 数据驱动图表 | **D3.js**（按需引入子模块） | 层剖面墙、路由热力图、权重曲线，SVG 可控性最好 |
| 高帧率矩阵动画 | **Canvas / 复用 p5.js** | 矩阵运算动画、状态递推 |
| 代码高亮 | **shiki** | 构建时高亮 → 零运行时 JS 开销 |
| 内容数据化 | **`generateStaticParams` + JSON/TS 数据文件** | 31 个详情页共用模板 |
| 重计算 | **Web Worker** | 任何 >50 ms 的同步计算一律下沉 |
| 进度存储 | **localStorage** | 算子掌握进度，无后端 |
| 图片处理（构建前） | **Pillow / libwebp**（见 §8） | 101 MB → 2.5 MB 的压缩流水线 |
| PDF 提图 | **PyMuPDF** 或 `pdfimages` | 从官方技术报告提取 Figure（V4.1 Flash 的 Figure 3） |
| 3D 张量（可选） | three.js | 仅 2–3 处，按需懒加载；**默认不引入** |

**明确不引入**：任何需要后端的方案（判题、模型推理、数据库）。核心原则是**纯静态、零运维、GitHub Pages 可直接托管**。

### 7.2 工程结构

```
seeing-llm/
├── src/
│   ├── app/
│   │   ├── page.tsx                        首页
│   │   ├── models/page.tsx                 全景对比表（A1）
│   │   ├── models/[slug]/page.tsx          ← 3 模型解剖页（模板 + generateStaticParams）
│   │   ├── modules/[slug]/page.tsx         ← 14 模块详解页
│   │   ├── compare/moe|kv-cache/page.tsx   ← 2 个横向对比专题（A4）
│   │   ├── timeline/page.tsx               演进时间轴（A5）
│   │   ├── pipeline/page.tsx               训练流水线总览（B1）
│   │   ├── pipeline/[stage]/page.tsx       ← 8 阶段详解页（B2）
│   │   ├── lab/tokenizer|nexttoken|cost/page.tsx  ← 3 个实验室（B3）
│   │   ├── ops/page.tsx                    20 算子清单（C1）
│   │   ├── ops/[slug]/page.tsx             ← 20 算子详解页（C2）
│   │   └── map/page.tsx                    概念地图
│   ├── content/                            ★ 内容数据层（核心）
│   │   ├── models.ts                       12 模型规格 + 3 模型的层结构 + 关联算子
│   │   ├── modules.ts                      14 模块的说明 + 公式 + 代码 + 关联算子
│   │   ├── oplist.ts                       20 算子的元信息 + 形状 + 陷阱
│   │   ├── stages.ts                       8 训练阶段
│   │   ├── timeline.ts                     演进节点
│   │   └── hotzones.ts                     ★ 架构图热区坐标（百分比）
│   │   └── sources.ts                      ★ 每项数据的官方来源 URL + 截止日期
│   ├── components/
│   │   ├── viz/                            可视化原语（★ 可复用资产）
│   │   │   ├── LayerWall.tsx               单模型层剖面墙
│   │   │   ├── DualLayerWall.tsx           ★ 双模型层墙（K3 ↔ Qwen3.8）
│   │   │   ├── HotzoneFigure.tsx           ★ 位图 + 热区叠层
│   │   │   ├── TensorFlow.tsx              形状流转
│   │   │   ├── MatrixStep.tsx              数值分步执行器（20 页共用）
│   │   │   ├── StateRecurrence.tsx         KDA / Gated DeltaNet 状态递推
│   │   │   ├── RouterHeatmap.tsx           MoE 路由热力图
│   │   │   ├── AttnHeatmap.tsx             注意力权重
│   │   │   ├── SamplingPlayground.tsx      采样策略（B3 与 C2 共用）
│   │   │   ├── RopeRotation.tsx            RoPE 旋转
│   │   │   └── FunctionCurve.tsx           激活函数曲线
│   │   └── OpLink.tsx                      ★ 三轨交叉链接卡片
│   └── lib/
├── public/
│   ├── figs/                               ★ 压缩后的架构图（约 2.5 MB）
│   │   ├── kimi-k3/                        1600w + 2400w + thumb
│   │   ├── deepseek-v41-flash/
│   │   └── qwen-3.8/
│   ├── data/                               预计算 JSON（体积受控）
│   └── scripts/analytics.js（复用）
└── scripts/
    ├── ingest_figs.py                      ★ 从官方源/Pdf 采集原图
    ├── compress_figs.py                    ★ 压缩流水线（核心）
    ├── annotate_hotzones.py                ★ 热区坐标标注辅助
    ├── gen_layer_wall.py                   层序列 JSON
    ├── gen_nexttoken.py                    预计算 logits
    └── gen_op_fixtures.py                  20 个算子的测试 fixture
```

> **可复用资产的分水岭**：`src/components/viz/` 里的原语。20 个算子页不写 20 套代码，而是**由 8–10 个原语组合**而成（`MatrixStep` 用约 12 次、`TensorFlow` 用 20 次、`FunctionCurve` 用约 5 次）。这是控制工程量的关键。

### 7.3 性能预算（硬约束）

| 指标 | 预算 | 依据 |
|---|---|---|
| 首页 JS（gzip） | < 150 KB | 与 seeing-single-cell 对齐 |
| **单页首屏数据（含图片）** | **< 400 KB** | 第六章 4.6 MB 的教训 |
| **单张架构图（页面内显示）** | **< 200 KB** | 实测 1600px + WebP q80 = 188 KB ✅ |
| 仓库总图片体积 | < 4 MB | 实测压缩后约 2.5 MB ✅ |
| 交互组件首次可交互 | < 1.5 s | 静态优先，交互后计算 |
| 任何主线程同步计算 | < 50 ms | 超出即下沉 Worker 或预计算 |
| Lighthouse 性能分 | ≥ 90 | 静态导出应能轻松达标 |

**图片流水线的硬规则**（实测得出）：

1. **先降分辨率，再选格式**——10850px → 1600px 就吃掉 87% 的体积；格式只贡献剩下的 13%
2. 每个模型输出**三档**：`thumb`（800px，列表用）/ `md`（1600px，页面内显示）/ `xl`（2400px，点击放大）
3. 格式：**WebP 为主**（q80），`<picture>` 里附 AVIF（再省约 35%）；原始 JPG **不入库**（写进 `.gitignore`）
4. 图片一律**懒加载** + `width`/`height` 或 `aspect-ratio` 占位，避免 CLS
5. "查看官方高清原图"走**外链**（指回 HF 模型卡 / 技术报告），不占本站流量
6. ⚠️ **注意 Pillow 的 `DecompressionBombWarning`**：10850×12619 的图会触发像素上限警告，流水线里需显式放开（`Image.MAX_IMAGE_PIXELS = None`）或分块处理

### 7.4 合规与致谢

**这是 v2 需要特别提醒的一节** —— 焦点三模型的许可各不相同：

| 模型 | 许可 | 本站需注意 |
|---|---|---|
| **Kimi K3** | **Kimi K3 License（非 MIT/Apache）** | MaaS 业务 12 个月营收 > 2000 万美元需另签协议；月活 > 1 亿或月营收 > 2000 万美元的产品需显著署名。**本站为教育/非商业，不涉及 MaaS，属于可自由使用的范畴** —— 但仍应在致谢页明确标注许可名称与链接 |
| **DeepSeek V4.1 Flash** | 已在 HF 开源 + 技术报告 | 引用架构图与规格，标注来源 |
| **Qwen3.8** | 权重已在 ModelScope 开放 | 引用时标注来源与官方博客链接 |
| MiniMind | Apache 2.0 | 可自由引用 |
| TorchCode | 开源 | 算子清单引用 + 页面导流回原站 |

**具体做法**：

- 架构图**压缩后本地引用**（教育用途 + 明确标注来源 + 提供官方原图外链）
- 每项规格数字在 `sources.ts` 里挂**官方 URL + 数据截止日期**，页面上渲染为角标
- 设统一"**致谢与来源**"页，逐条列出参考项目与授权方式：
  `CalvinXKY/InfraTech`（历史模型卡 + 专题文章）、`jingyaogong/minimind`（Apache 2.0）、`duoan/TorchCode`、Moonshot AI / DeepSeek / 阿里 官方技术报告与模型卡
- **自绘 SVG 升级仍保留在路线图上**（P5+）：它同时解决版权与交互两个问题，只是本期不阻塞

---

## 八、数据生产流水线

沿用第六章验证有效的"**离线预计算 → 小 JSON → 前端只读**"模式，并新增图片流水线。

### 8.1 图片流水线（★ 本期新增的重点）

| 步骤 | 脚本 | 说明 |
|---|---|---|
| ① 采集 | `ingest_figs.py` | 从 HF 模型卡 / ModelScope / InfraTech 抓原图；V4.1 Flash 的 Figure 3 从技术报告 PDF 用 PyMuPDF 提取（`page.get_images()` 或区域裁剪，注意选**矢量图而非截图**以保证清晰度） |
| ② 压缩 | `compress_figs.py` | 每个模型产出 `thumb/md/xl` 三档 WebP（+ AVIF），自动报告"原图→压缩后"的体积表（同 §1.4 的表） |
| ③ 标注热区 | `annotate_hotzones.py` | 起一个本地页面把原图铺开，鼠标框选模块区域，输出百分比坐标到 `hotzones.ts`。**这个工具让 14 个模块的热区标注从"量像素"变成"点几下"** |
| ④ 校验 | 构建期脚本 | 检查：图片是否超预算、热区是否越界/重叠、`sources.ts` 是否每项都有 URL |

### 8.2 数据流水线

| 脚本 | 输入 | 输出 | 用途 |
|---|---|---|---|
| `extract_specs.py` | 官方模型卡 / 技术报告 | `content/models.ts` | 对比表 + 规格卡 |
| `gen_layer_wall.py` | 层配置（K3 的 23×4+1、Qwen3.8 的 23×4、V4.1 的 20+20） | 层序列 JSON（约 10 KB） | 层剖面墙 + 双模型层墙 |
| `build_tokenizer_samples.py` | 预置文本 + tokenizer | 分词结果 JSON（约 50 KB） | 分词实验室 |
| `gen_nexttoken.py` | 小模型权重 → top-k logits | logits JSON（约 100 KB） | 下一个 token 预测 |
| `gen_op_fixtures.py` | 20 个算子的测试输入 | 小矩阵 fixture JSON（每个 < 2 KB） | 算子数值分步执行 |

> **为什么坚持离线预计算**：这是第六章用 4.6 MB 主线程 PCA 换来 126 亿次运算假死的血泪教训。凡是"输入确定、结果确定"的内容，一律离线算好。

---

## 九、开发里程碑（P0–P5，聚焦版）

| 期 | 内容 | 交付物 | 验收标准 |
|---|---|---|---|
| **P0 骨架 + 图片流水线** | 复用 seeing-single-cell 底座；新设计系统；**图片压缩流水线跑通（101 MB → 2.5 MB）**；部署链路打通 | 首页 + 空壳导航 + 图片资产 + 自动部署 | 线上可访问；CI 一次通过；访问统计生效；**仓库图片总体积 < 4 MB** |
| **P1 Kimi K3 垂直切片** | 打透**一个模型全链路**：A1 对比表 + K3 解剖页（含位图热区）+ 层剖面墙 + KDA 模块页 + AttnRes 模块页 + 4 个算子页（SDPA / MHA / RoPE / Linear Attention）+ 时间轴 KDA 节点 | 约 12 页 | 端到端体验成立；**证明"三轨联动"可行**；单页首屏 < 400 KB |
| **P2 算子铺开** | 轨道 C 完整 20 页（8–10 个可视化原语 + 数据驱动） | 21 页 | 20 个算子全部可交互；进度追踪可用 |
| **P3 补齐另两个模型 + 横向专题** | V4.1 Flash 解剖页（CED / CSA2 / Engram / mHC / DSpark / ViT 6 个模块页）+ Qwen3.8 解剖页（Gated DeltaNet / Gated Attention / MTP 3 个模块页）+ **双模型层墙** + **2 个横向对比专题** + 演进时间轴 | 约 18 页 | **"同一问题的三种解法"呈现完整**；三模型层墙对齐正确 |
| **P4 训练流程** | 流水线总览 + 8 阶段页 + 3 个实验室 | 12 页 | 开销计算器数字与 MiniMind 实测一致 |
| **P5 收尾** | 概念地图、术语表、致谢、SEO、剩余 21 个算子、性能优化 | 约 8 页（+21 可选） | Lighthouse ≥ 90；全站无死链 |

**排序理由**：

- **P0 就把图片流水线跑通**——因为这是您本轮新选的策略，且是全站资产的前提，越早验证越好（实测已证明可行，但要在真实仓库流程里跑一遍）
- **P1 先做完整垂直切片**（单模型全链路），用最小成本验证信息架构与交互模式是否成立，避免在错误方向上铺量
- **P2 先于 P3**：算子页数量多但每个最小、模式最统一，能最快积累可视化原语，反过来支撑 A、B 两轨
- **P3 把"三种解法"的对比一次做完**——这三个模型放在一起才是本站的护城河，不要拆散

---

## 十、风险与取舍

| 风险 | 影响 | 应对 |
|---|---|---|
| **位图不可交互、模块位置标错** | 点错模块比不能点更糟 | 热区坐标集中在 `hotzones.ts`，用 `annotate_hotzones.py` 可视化标注；构建期校验越界/重叠；预留自绘 SVG 升级路径 |
| **最新模型数据持续变动** | V4.1 Flash 刚发布 4 天、Qwen3.8 更小版本预告中（27B 在路上），数字会过时 | `sources.ts` 统一管理来源 + 截止日期；页面上显式渲染"数据截止 2026-09-14"；规格集中一处，更新即全站生效 |
| **官方原图获取不到**（InfraTech 无 Qwen3.8 / V4.1 Flash） | 焦点模型缺主图 | 已规划多源采集（HF 模型卡 / ModelScope / 技术报告 PDF 提图）；实在没有再退回"结构示意图 + 官方外链" |
| **内容准确性（最需谨慎）** | 讲错比不讲更糟 | 每个结论标注来源；区分"官方公布"与"社区解读"；不确定处标注"示意"（如 K3 的 AttnRes 官方承认**尚无独立论文完整披露**）；§1.2 里"Gated MLA 23 vs 24 层"这类口径差异一律标注"以官方技术报告为准" |
| **主线程计算陷阱** | 复现第六章的"加载失败" | 性能预算写进验收标准（§7.3）；Code Review 检查项 |
| **与 TorchCode 同质** | 用户觉得"重复造轮子" | 明确定位差异：**它是练习场，本站是理解场**；页面导流到 TorchCode；本站独有"可视化 + 三轨联动 + 三模型对比" |
| **双模型层墙的对齐假设被推翻** | 若 K3 的层布局另有官方口径，对齐图会错 | 这是**基于官方 README 数字推算**的结果（23×4+1=93 ✅、23×4=92 ✅ 两者都严丝合缝），但仍需在 P3 用 `k3_tech_report.pdf` 与 Qwen 模型卡二次核对；图中标注"依据官方规格推算" |
| **工程量（约 60 页）** | 拖长周期 | 31 页走数据驱动模板；分期上线，每期独立可用 |

**明确的取舍**：

- ❌ 不做在线判题（需后端）
- ❌ 不做真实模型推理（体积与算力不允许）→ 用预计算 logits 演示
- ❌ 不做 3D 炫技（除非个别页确有必要）
- ❌ **本期不做 11 个模型的深度解剖**（聚焦 3 个，其余只在对比表里列数字）
- ✅ 做深"看懂了"这件事：每个概念都要有一个可操作的交互
- ✅ 做深"**同一问题的三种解法**"：这是本站最原创的部分

---

## 十一、补充决策（已确认，2026-09-14）

主体决策已按您的反馈落地，以下 4 项执行细节已于本轮确认，**方案规格就此冻结**：

1. **模型版本基准 = Qwen3.8**（非 Qwen3.5）。
   > 说明：您原消息正文写"Qwen3.8"、末尾写"Qwen3.5"，两者矛盾。经核实 **Qwen3.8-2.4T-A95B 已开放权重**，比 Qwen3.5 更新，且其 92 层骨架与 K3 同构（23×4）。→ **以 Qwen3.8 为准**，§1.2 规格表、轨道 A 模块页、双模型层墙均按此执行。若后续确需回退到 3.5，需改写 §1.2 + 轨道 A 共 3 处。

2. **Qwen3.8 的 27B 小尺寸版本**：**对比表先留占位行**（标注"待官方发布"），发布后补数据，**不阻塞 P3 进度**。占位行写入 §4 的 12 模型全景表。

3. **DeepSeek V4 Pro**：**只在对比表保留一行历史记录**，标注"2026-09-14 12:00 起全部路由至 V4.1 Flash"，**不做独立解剖页**。对比表共 13 行（12 模型 + 1 历史行）。

4. **剩余 21 个算子（⭐ 10 + 💡 11）**：**列成「待补清单」长期滚动**。P2 只交付 20 个（16 🔥 + 4 补），清单在站点上公开挂出（可点、显示"待补"状态），按访问热度决定补写顺序。**P5 不承诺全量完成**，只承诺 P2 的 20 个 + 清单上线。

---

*方案版本 v2.1 · 2026-09-14 · 已并入您的 7 项决策 + 4 项补充确认 · 规格冻结，等待 P0 放行*
